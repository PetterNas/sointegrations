#setLanguageLevel 3;
#include"chatbotConfig";

Void sendFirstMessage(String chatSessionId, String customerFirstName)
{
    String greetingMsg = "";
    String greetingMsgNoName = "";

  if(customerFirstName.isEmpty())
  {
  	addChatMessage(chatSessionId.toInteger(),greetingMsgNoName,1,"botName",0,"");
  }
  else
  {
  	addChatMessage(chatSessionId.toInteger(),greetingMsg,1,"botName",0,"");
  }
}
